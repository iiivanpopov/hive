{
  "name": "hive-backend",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/app/client.d.ts",
      "import": "./dist/app/client.js"
    }
  },
  "main": "./dist/app/client.js",
  "types": "./dist/app/client.d.ts",
  "scripts": {
    "dev": "NODE_ENV=development bun --hot ./src/app/entrypoint.ts",
    "prod": "NODE_ENV=production bun ./dist/index.js",
    "build:dev": "bun --bun tsgo -b && NODE_ENV=development bun build.script.ts && bun build:client",
    "build:prod": "NODE_ENV=production bun build.script.ts && bun build:client",
    "build:client": "bun --bun tsgo -p ./tsconfig.build.json",
    "lint": "bun --bun eslint .",
    "db:studio": "bun --bun drizzle-kit studio",
    "db:push": "bun --bun drizzle-kit push",
    "db:generate": "bun --bun drizzle-kit generate"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.2.0",
    "drizzle-orm": "^1.0.0-beta.2-58a4521",
    "hono": "^4.11.0",
    "pino": "^10.1.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@scalar/hono-api-reference": "^0.9.28",
    "@standard-schema/spec": "^1.0.0",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:",
    "drizzle-kit": "^1.0.0-beta.2-58a4521",
    "eslint": "catalog:",
    "hive-shared": "workspace:*",
    "hono-openapi": "^1.1.2",
    "pino-pretty": "^13.1.3"
  }
}
